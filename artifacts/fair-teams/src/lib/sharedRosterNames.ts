export type SharedSummaryNameSource = "stored" | "fallback";

// Existing persisted compatibility defaults. These must remain locale-neutral.
export const DEFAULT_FIREBASE_SHARED_GROUP_NAME = "My Stripes group";
export const DEFAULT_FIREBASE_SHARED_ROSTER_NAME = "Shared roster";
